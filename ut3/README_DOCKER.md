# Aplicación Nginx + ngx_small_light

## Imagen Docker: aaron_small_light

### Para usar la imagen:

1. **Cargar la imagen:**
   ```bash
   docker load -i aaron_small_light.tar
   ```

2. **Ejecutar el contenedor:**
   ``` bash
   docker run -d -p 8080:8000 --name small_light_app aaron_small_light
   ```


